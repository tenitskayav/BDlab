--
--Выведите наименования всех объектов клуба заглавными буквами, если они содержат в названии слово ‘Tennis’
USE cd;
SELECT UPPER(facility)  AS 'Название объекта' FROM facilities WHERE facility LIKE BINARY '%Tennis%';